# NPOS 預約簡訊多模板｜後端實作規格

> 最新版本：v1.7　2026-07-07

> 本文件為工程實作用權威規格，隨專案進度持續更新，異動請見下方修改紀錄。PM／需求溝通用途請改看 `後端規格書（PM視角）.md`。

> ⚠️ **派案前必讀**：本文件內所有資料表／API（`sms_templates`、`appointment`、`sms_send_log` 等）都是**建議設計，尚未與後端工程師核對過**，也沒有對照過公司既有 POS 架構逐項確認。「模板」這整套系統在真實 POS 架構裡完全沒有對應表，是全新設計；正式派案時請明確告知後端工程師這是建議稿，需依實際 POS schema（`docs/pos_schema_exports/`）重新評估欄位/型別/命名，不是已拍板的契約。

### 修改紀錄

| 版本 | 日期 | 異動摘要 |
| --- | --- | --- |
| v1.0 | 2026-06-26 | 初版：7 個系統參數、模板 CRUD、個人偏好、發送 Log |
| v1.1 | 2026-06-30 | 新增 診間／時段／派號 三個系統參數；補入派號推算公式 |
| v1.2 | 2026-06-30 | 新增輸入驗證規則、授權驗證流程、發送安全、稽核日誌 |
| v1.3 | 2026-06-30 | 全讀 / 只寫自己的模板隔離設計；補排程管理端點、Log 查詢、錯誤碼速查、沙箱模式 |
| v1.4 | 2026-07-07 | 依據最新原型設計：移除跨店瀏覽與複製模板功能，聚焦於本門市獨立自訂模板與系統內建模板。 |
| v1.5 | 2026-07-07 | 修正派號規則：原「依預約時間換算、每 15 分鐘一格」公式僅為原型示意假資料，非後端實際規則；改為依同一診間/時段內預約建立順序自動遞增奇數序號（後端已實作）。 |
| v1.6 | 2026-07-07 | 依 `後端規格書（PM視角）.md` v1.1 同步：新增同日多預約僅最早一筆可發送/取消的規則、取消最早預約時的補位順序、重新發送等於重新呼叫 send API（非重寄舊 log）、拒收覆蓋發送需記錄於 `sms_send_log`。 |
| v1.7 | 2026-07-07 | 使用者確認：派號、同日多預約僅最早一筆可操作等規則後端已實作完成；「診別（早/午/晚，即時段判斷）」維持既有時間換算公式不變，需正式落地為後端程式，非邏輯變更。本文件內 `appointments`/`stores`/`sms_send_log` 等為示意用資料表，實際串接時請對照公司既有 POS 架構（`docs/pos_schema_exports/`）——例如 `CC_ProjectShortMessage`（既有專案簡訊發送紀錄表）、`AP_DRSchdule`/`AP_BASchdule`（既有預約排程表），兩者目前都沒有模板系統或診間/時段/派號欄位，屬於全新設計；實際欄位命名/型別對照需與後端工程師依真實 schema 確認，本文件不代為假設細節。 |

---

## 一、系統參數白名單

`body` 欄位僅允許以下 token，其他 `{{...}}` 格式儲存時回 400。

| Token | 說明 | 解析來源 |
|---|---|---|
| `{{會員姓名}}` | 會員全名 | `appointments.member_name` |
| `{{門市名稱}}` | 門市名稱 | `stores.name` |
| `{{預約日期}}` | 格式 `MM/DD（星期）` | `appointments.date` |
| `{{預約時間}}` | 格式 `HH:MM` | `appointments.time` |
| `{{療程}}` | 施做療程項目 | `appointments.treatment_name` |
| `{{診間}}` | 診療室（診一、診二 …） | `appointments.room` |
| `{{時段}}` | 早診 / 午診 / 晚診（由時間推算，見第二節） | 後端計算 |
| `{{派號}}` | 依預約建立順序自動遞增之奇數序號（見第二節） | 後端計算（已實作） |
| `{{門市地址}}` | 完整地址 | `stores.address` |
| `{{門市電話}}` | 含區碼，如 `02-XXXX-XXXX` | `stores.phone` |

---

## 二、時段判斷與派號規則

### 2.1 時段判斷

```
appointment.time（HH:MM）→ 換算成分鐘數 total

total < 13:00（780 分）  → 早診
total < 17:00（1020 分） → 午診
total ≥ 17:00            → 晚診
```

> **v1.7 確認**：「時段」與「診別」是同一件事的兩種說法，此公式維持不變，需正式落地為後端程式（先前只在原型 JS 裡）。

### 2.2 派號規則（後端已實作，非時間換算）

派號**不是由預約時間換算**，而是依**同一診間／同一時段內，預約建立的先後順序**，由後端自動遞增產生奇數序號：

```
同一診間 + 同一時段內，依預約建立順序遞增：
派號 = 1 + (該診間/時段內建立順位 − 1) × 2   // 奇數序列：1、3、5、7 …
```

範例（同一診間、同一時段，依建立順序）：

| 建立順位 | 派號 |
|---|---|
| 第 1 筆 | 1 |
| 第 2 筆 | 3 |
| 第 3 筆 | 5 |
| 第 4 筆 | 7 |

> ⚠️ **修正說明（v1.5）**：v1.1～v1.4 版本曾記錄「依預約時間換算、每 15 分鐘一格」的公式，該公式僅為原型（`prototypes/預約操作模擬.html` 內 `queueNoFromTime()`）用來產生示意假資料，並非後端實際規則，已於此版修正為依建立順序遞增的實作邏輯。

---

## 三、資料模型（重點欄位）

### `sms_templates`

| 欄位 | 型別 | 說明 |
|---|---|---|
| `id` | VARCHAR(36) PK | 系統內建前綴 `builtin_`，自訂前綴 `c_` |
| `store_id` | INT FK | NULL = 全系統共用 |
| `name` | VARCHAR(64) | 同門市範圍唯一 |
| `body` | TEXT | 含 `{{Token}}` 的模板內容 |
| `enabled` | BOOLEAN | false 時不出現在前端下拉 |
| `is_builtin` | BOOLEAN | true 時不可刪除、不可改 body |

### `sms_user_preferences`

| 欄位 | 型別 | 說明 |
|---|---|---|
| `user_id` | INT PK | |
| `pinned_ids` | JSON Array | 釘選模板 ID |
| `recent_ids` | JSON Array | 最近使用（上限 5） |
| `last_used_id` | VARCHAR(36) | 上次套用的模板 ID |

### `sms_store_settings`

| 欄位 | 型別 | 說明 |
|---|---|---|
| `store_id` | INT PK | |
| `sms_enabled` | BOOLEAN | 全域發送開關 |
| `include_tele` | BOOLEAN | 包含電訪推廣附加語 |

### `sms_send_log`

| 欄位 | 型別 | 說明 |
|---|---|---|
| `appointment_id` | INT FK | 用於查手機號碼，不直接儲存完整號碼 |
| `recipient_masked` | VARCHAR(20) | 隱碼：`09xx-ooo-NNNN`（僅末四碼） |
| `content` | TEXT | 已解析的最終簡訊文字 |
| `segments` | INT | 簡訊則數 |
| `scheduled_at` | DATETIME | NULL = 立即發送 |
| `status` | ENUM | `pending` / `sent` / `failed` |
| `override_opt_out` | BOOLEAN | 是否為「會員已設定拒收，人員仍強制送出」；預設 `false`，供稽核追查覆蓋發送 |

---

## 三之一、模板可見性與所有權 (已調整：移除跨店功能)

採「**本門市獨立運作**」設計——各門市只能查看及管理本門市與系統內建之模板，無法跨店瀏覽其他門市的模板。

| 操作 | 規則 |
| --- | --- |
| **讀取（GET）** | 僅回傳本門市與系統內建的模板，其他門市之模板不予回傳 |
| **新增（POST）** | 只能建立 `store_id = session.store_id` 的模板 |
| **編輯 / 刪除** | 只能操作 `template.store_id === session.store_id` 的模板，其他均 403 |
| **系統內建** | `store_id = NULL`，任何門市唯讀；系統管理員可改 `enabled` / `body` |

---

## 四、API 端點速查

| 動作 | Method | 路徑 | 權限 |
|---|---|---|---|
| 取得本門市與系統內建模板清單 | GET | `/api/sms-templates` | 全員 |
| 新增自訂模板 | POST | `/api/sms-templates` | 門市主管↑ |
| 更新模板 | PUT | `/api/sms-templates/:id` | 門市主管↑（只能改自己門市的；內建僅可改 `enabled`） |
| 刪除自訂模板 | DELETE | `/api/sms-templates/:id` | 門市主管↑（只能刪自己門市的；內建不可刪） |
| 取得個人偏好 | GET | `/api/user/sms-preferences` | 全員 |
| 更新個人偏好 | PUT | `/api/user/sms-preferences` | 全員 |
| 發送簡訊 | POST | `/api/sms/send` | 全員 |
| 查詢待發排程 | GET | `/api/sms/scheduled?appointment_id=` | 全員（只回傳自己門市的） |
| 取消排程發送 | DELETE | `/api/sms/scheduled/:log_id` | 全員（status 必須為 pending，否則 409） |
| 查詢發送記錄 | GET | `/api/sms/logs?appointment_id=&page=` | 門市主管↑；含隱碼手機；不含完整號碼 |
| 取得門市設定 | GET | `/api/store/settings` | 全員 |
| 更新門市設定 | PUT | `/api/store/settings` | 門市主管↑ |

### POST `/api/sms/send` 重點欄位

```json
{
  "appointment_id": 10042,
  "content": "已解析的最終文字（前端人員可能已手動修改）",
  "scheduled_at": null
}
```

- `appointment_id` → 後端自行取手機號碼，前端不傳
- `content` → 長度上限 670 字；超過回 400
- `scheduled_at` 為 null 或省略 → 立即發送
- 不記錄本次套用的是哪個模板：前端套用模板後即以最終解析文字送出，`sms_send_log` 不存 `template_id`，簡訊詳情也不顯示「使用模板」（2026-07-13 定案取消，範圍含前後端）

### 4.1 同日多預約：僅最早一筆可發送／取消

同一會員、同一天若有多筆**未取消**的預約，`POST /api/sms/send` 與 `DELETE /api/sms/scheduled/:log_id` 都只接受**時間最早**那一筆的 `appointment_id`；對非最早的預約呼叫這兩個端點一律回 409（`error: "not_earliest_appointment"`）。前端本身會鎖住非最早預約的操作入口，但後端仍需獨立驗證，避免繞過前端直接打 API。判斷「最早」只看該會員同日各筆預約本身是否為 `cancelled`，跟簡訊自己的狀態（例如簡訊已被取消）無關。

### 4.2 取消預約時的補位（前端協調的兩步驟，非單一端點）

若使用者要取消同會員同日多筆預約中「最早」的一筆，且同日還有其他有效預約，前端會改為對**次早**那一筆呼叫 `POST /api/sms/send` 讓人員確認發送內容；等這次發送成功建立（拿到 `log_id`）之後，前端才呼叫（既有、非本文件範圍的）預約取消 API 把原本最早那筆的 `appointment_status` 改為 `cancelled`。**後端不需要新增「轉移」端點**，只需確保「先送出新對象的簡訊、成功後才取消原預約」這個順序被遵守，避免中途失敗導致兩邊都取消或都沒發送的不一致狀態。

### 4.3 重新發送＝重新呼叫 send API，非重寄舊 log

`已取消`／`發送失敗` 的簡訊要「重新發送」時，前端會重新走一次模板套用優先序（見 `後端規格書（PM視角）.md` 2.3 節）與情境判斷（拒收／同日排程衝突），再呼叫 `POST /api/sms/send`。重新發送**不是**針對舊 `log_id` 做動作，而是對同一 `appointment_id` 建立一筆全新的 `sms_send_log` 記錄；`content` 很可能跟上一筆不同，後端不需要處理「沿用舊內容」的邏輯。

---

## 五、個資處理規則

| 規則 | 說明 |
|---|---|
| 手機號碼 | 由後端從 `appointment_id` 關聯取得；不接受前端傳入完整號碼 |
| Log 儲存 | `recipient_masked` 僅儲存末四碼（`09xx-ooo-NNNN`） |
| API Response | 不回傳完整手機號碼 |
| 會員拒絕簡訊 | 後端判斷拒絕狀態，回 400 並說明原因；前端可加 `"force": true` 強制送出；此時 `sms_send_log.override_opt_out` 需寫 `true` 供稽核追查 |

---

## 六、權限矩陣

| 功能 | 一般人員 | 門市主管 | 系統管理員 |
|---|---|---|---|
| 查看模板、套用發送 | ✅ | ✅ | ✅ |
| 新增 / 編輯 / 刪除自訂模板 | ❌ | ✅ | ✅ |
| 調整門市簡訊全域開關 | ❌ | ✅ | ✅ |
| 修改系統內建模板 body | ❌ | ❌ | ✅ |
| 查看發送 Log | ❌ | ✅ | ✅ |

---

## 七、輸入驗證規則

### 模板 CRUD

| 欄位 | 規則 | 錯誤 |
| --- | --- | --- |
| `name` | 1–64 字；同 `store_id` 範圍內唯一 | 空值/超長 → 400；重複 → 409 |
| `body` | 10–500 字；只允許白名單 token | 長度違規 → 400；非白名單 token → 400 |
| `enabled` | Boolean，必填（不接受 null） | → 400 |

### 發送 API

| 欄位 | 規則 | 錯誤 |
| --- | --- | --- |
| `content` | 1–670 字 | → 400 |
| `scheduled_at` | 若非 null，必須為未來時間 | → 422 |
| `appointment_id` | 必須存在於資料庫 | → 404 |

### 刪除模板前置

刪除時**不攔截**（前端對 `last_used_id` / `pinned_ids` 已有 fallback）。
但若有使用者仍引用此模板，Response 可附加 `warning` 欄位說明影響範圍：

```json
{
  "deleted": true,
  "warning": "2 位使用者的上次套用模板已重置為預設"
}
```

---

## 八、授權驗證流程

### Session Claims（後端驗證所需）

每個 API 請求的 token 必須可解析出以下欄位：

| Claim | 說明 |
| --- | --- |
| `user_id` | 操作者帳號 ID |
| `store_id` | 操作者所屬門市 ID |
| `role` | `staff`（一般人員）/ `manager`（門市主管）/ `admin`（系統管理員） |

### 驗證原則

1. **不信任前端傳入的 store_id**：所有操作以 token 中的 `store_id` 為準。
2. **讀取開放、寫入限制**：
   - `GET` 類端點回傳全門市模板，不依 store_id 過濾。
   - `POST / PUT / DELETE` 類端點：驗 `template.store_id === token.store_id`，否則 403。
   - `store_id = NULL`（系統內建）：任何門市的 PUT/DELETE 均 403；只有 `role = admin` 才例外。
3. **Role 驗證在後端執行**，不依賴前端路由限制作為唯一防線。

### 範例：PUT /api/sms-templates/:id

```
1. 解析 token → 取得 user_id、store_id、role
2. 查詢 template WHERE id = :id
3. 若 template 不存在 → 404
4. 若 template.store_id ≠ token.store_id（含 NULL ≠ X）→ 403
5. 若 role < manager → 403
6. 若 template.is_builtin = true 且嘗試改 body/name → 403（admin 例外）
7. 執行更新
```

---

## 九、發送安全

### 重複發送防護（Dedup）

同一 `appointment_id` 在 **5 分鐘內**再次發送，回 409：

```json
{
  "error": "duplicate_send",
  "prev_log_id": 9981,
  "message": "此預約已於 5 分鐘內發送過簡訊"
}
```

若前端人員確認需要強制重發，Request Body 加 `"force": true` 即可覆蓋。

### 發送速率限制（Rate Limit）

| 範圍 | 上限 | 超過時回應 |
| --- | --- | --- |
| 單一門市 / 小時 | 200 則（建議起點，上線後依實際量調整） | 429 + `Retry-After` header |

### Gateway 降級

若 SMS Gateway 連續 3 次呼叫失敗，後端將 `status` 改為 `failed`、寫入 `error_msg`，**不拋 exception 給前端**，改回 `{ "status": "failed", "log_id": ... }`，由前端顯示錯誤提示並讓人員決定是否重試。

### 測試 / 沙箱模式

後端讀環境變數 `SMS_SANDBOX=true` 時，跳過 Gateway 實際呼叫，log 寫入 `status=sent`（mock），適用 staging / 測試環境，避免真實發送。

---

## 十、稽核日誌

`sms_templates` 的 CREATE / UPDATE / DELETE 操作需寫入 audit log，供日後追查「誰改了這條簡訊模板」。

| 欄位 | 型別 | 說明 |
| --- | --- | --- |
| `actor_id` | INT | 操作者 user_id |
| `action` | ENUM | `create` / `update` / `delete` |
| `target_id` | VARCHAR(36) | 被操作的 template id |
| `payload` | JSON | 變更前後 diff（含 name、body、enabled 的 before/after） |
| `created_at` | DATETIME | 操作時間 |

Log 保存期限同 `sms_send_log`：12 個月。

---

## 十一、錯誤碼速查

| 狀態碼 | 觸發情境 |
| --- | --- |
| 400 | 欄位格式 / 長度違規；`body` 含非白名單 token；`content` 超 670 字；會員拒絕簡訊 |
| 403 | 角色不足；嘗試寫入他門市或系統內建模板；嘗試改內建模板 `body` |
| 404 | `appointment_id` / `template_id` / `log_id` 不存在 |
| 409 | 模板名稱在同門市重複；5 分鐘 dedup 觸發；取消非 `pending` 排程；對非最早同日預約呼叫 `/api/sms/send` 或 `/api/sms/scheduled/:log_id`（`not_earliest_appointment`） |
| 422 | `scheduled_at` 為過去時間 |
| 429 | 超過門市每小時發送速率上限（附 `Retry-After` header） |
| 503 | SMS Gateway 無法連線（後端 log 寫 `failed`；Response 含 `retry_after` 建議值） |

---

## 附：前端 localStorage 對照（原型用，上線改 API）

| Key | 說明 |
|---|---|
| `npos_tpl_custom` | 自訂模板清單（JSON Array） |
| `npos_tpl_pins` | 釘選模板 ID（JSON Array） |
| `npos_tpl_recent` | 最近使用（最多 5 筆） |
| `npos_tpl_last` | 上次套用的模板 ID |
| `npos_sms_enabled` | 全域開關（`"1"` / `"0"`） |
| `npos_sms_tele` | 電訪推廣開關（`"1"` / `"0"`） |
