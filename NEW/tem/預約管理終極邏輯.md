# 簡訊彈窗情境與文案維護說明（PM 版）

> 🎯 目標：  
> PM 只需要維護「幾種邏輯情境」的文字，  
> RD 會在不同操作（同群組 / 跨群組 / 單筆管理）重複使用這些文案。

---

## 1. 總原則

1. **預約不會被擋**
   - 所有門市都可以直接幫客人建立預約。
   - 簡訊規則只影響「要不要發簡訊、發給哪一筆」，**不會阻止預約建立或保留**。

2. **簡訊以 SYSGroup 為單位計算**
   - 同一位客戶 + 同一個 SYSGroup，只會有一筆「最早預約」可發簡訊，其餘為 `none`（或異常狀態）。

3. **不同 SYSGroup 彼此獨立**
   - 在 G2 做的任何操作（新增、取消），只會影響「G2 內部的簡訊狀態」，
   - **不會改動 G0 / G1 / 其他群組的預約與簡訊排程**。

4. **跨群組是後端概念，前台都是「當下門市」**
   - 使用者切到哪一個門市，就在那個門市底下操作預約。
   - 文案只會講「這筆預約 / 目前的預約安排」，**不會出現「群組 / SYSGroup」** 這種技術字眼。

5. **黑名單是共用規則**
   - 若勾選「黑名單」，所有彈窗提示最上方會多出一段黑名單警告，且樣式改為「引導取消」。

---

## 2. PM 需維護的「情境文案」一覽

> ✅ PM 只要維護這 9 種「邏輯情境」的文字即可。  
> ✅ 同群組 / 跨群組 / 單筆管理，都會共用這些文案。  
> 🚫 `文案代號` 請不要改，工程師會用這些 key 對應程式邏輯。

| # | 情境類型（PM 看懂就好） | 說明（邏輯情境） | 彈窗標題（✅可改） | 提示內容（✅可改） | 問句（✅可改，可留空） | 文案代號（🚫勿改） |
|---|------------------------|------------------|--------------------|--------------------|------------------------|---------------------|
| 1 | 成為目前最早預約（無舊排程） | 目前沒有任何會發簡訊的預約，這是第一筆「可以發簡訊」的預約。 | 發送預覽 | ✓ 將為此預約排程簡訊，成為目前最早的簡訊通知。 | 是否確認發送此預約的簡訊？ | scenario.becomeFirst.noPrevious |
| 2 | 成為目前最早預約（取代舊排程） | 已經有一筆較晚的排程，這筆會變成新的最早預約，取代原本那筆發簡訊。 | 發送預覽 | ⚠️ 將取代現有排程，原最早預約的簡訊將被取消。 | 是否確認發送此預約的簡訊？ | scenario.becomeFirst.replaceExisting |
| 3 | 同時間衝突：舊預約優先 | 已有同時間預約，依先來後到（ID 較小）優先，新預約不會發簡訊。 | ⚠️ 注意：時間衝突 | ✓ 根據「先來後到」規則，目前已有同時間的預約（ID 較小）將優先發送簡訊，此次新增不會發送簡訊。 | 是否要為這筆既有預約更新簡訊內容與發送時間？ | scenario.timeConflict.sameTime |
| 4 | 最早預約簡訊異常，提醒修正 | 目前應該發簡訊的「最早預約」是未發送 / 已取消 / 發送失敗，系統提醒修正。 | 發送預覽 | ✓ 系統偵測到最早預約簡訊異常（未發送/已取消/發送失敗），建議修正。 | 是否確認發送此預約的簡訊？ | scenario.earliest.abnormalFix |
| 5 | 新增較晚預約：已有更早負責發簡訊 | 已有更早的預約負責發簡訊，這筆只是新增預約，不會再發簡訊。 | 發送預覽 | ℹ️ 已有更早的預約負責發送簡訊，此次新增不會發送簡訊。 | 是否確認新增此預約？ | scenario.later.hasEarlier |
| 6 | 新增次早預約時發生時間衝突 | 新增的預約時間與其他預約相同，依先來後到（ID 較小）優先。 | ⚠️ 注意：時間衝突 | ✓ 根據「先來後到」規則，時間相同時優先處理 ID 較小的預約。 | 是否要為此筆既有預約更新簡訊內容與發送時間？ | scenario.timeConflict.secondCase |
| 7 | 取消最早後，有唯一補位預約 | 取消目前最早預約，只有一筆次早預約可補位發簡訊。 | 發送預覽 | ✓ 最早預約已取消，系統將為次早預約發送簡訊。 | 是否確認為補位預約發送簡訊？ | scenario.cancelEarliest.singleReplacement |
| 8 | 取消最早後，有多筆同時間補位 | 取消最早預約後，有多筆同時間補位候選，系統自動選 ID 最小者。 | 發送預覽 | ✓ 最早預約已取消，系統已自動選定補位預約（多筆同時間時優先處理 ID 較小者）。 | 是否確認為補位預約發送簡訊？ | scenario.cancelEarliest.multiReplacement |
| 9 | 取消最早後，沒有補位 | 取消最早預約後，沒有任何預約可以補位發簡訊。 | 提示 | ⚠️ 目前沒有其他預約可以補位。 |  | scenario.cancelEarliest.noReplacement |

---

## 3. 黑名單共用文案

| 類型 | 說明 | 提示內容（✅可改） | 文案代號（🚫勿改） |
|------|------|--------------------|---------------------|
| 黑名單警告前綴 | 勾選黑名單時，加在所有彈窗提示最上方的一句警告。 | ⚠️ 警告：此會員已被標記為黑名單，強烈建議取消發送！ | scenario.blacklist.warning |

> 工程實作：勾選黑名單時，會在任何情境的提示內容最前面插入這段文字，並將彈窗樣式改為「引導取消」。

---

## 4. 系統情境 → 文案對應表（同群組 / 跨群組）

> 這一區主要給 PM / RD 對齊：  
> 「系統在某個操作情境下，用的是上面哪一段文案」。

### 4.1 同群組操作（同一 SYSGroup 內）

| 系統情境代號（RD 使用） | 說明（同群組） | 使用文案代號 | 彈窗模式 |
|-------------------------|----------------|--------------|----------|
| addEarlier.noConflict | 同一群組內新增一筆「比目前最早更早」的預約，沒有時間衝突，會取代原排程。 | scenario.becomeFirst.replaceExisting | guide-confirm |
| addEarlier.conflict | 同一群組內新增的預約時間與原最早預約相同，新預約不發簡訊，舊預約優先。 | scenario.timeConflict.sameTime | info |
| addSameTime.conflict | 刻意新增一筆與現有預約同時間的預約，新預約固定不發簡訊。 | scenario.timeConflict.sameTime | info |
| addSecond.earliestAbnormal | 最早預約簡訊異常（未發送/取消/失敗），新增較晚預約時提醒修正最早。 | scenario.earliest.abnormalFix | guide-confirm |
| addSecond.earliestNormal | 最早預約簡訊正常，新增較晚預約只建立，不發簡訊。 | scenario.later.hasEarlier | guide-cancel |
| addSecond.conflict | 新增次早預約時，時間與其他預約相同，套用同時間衝突規則。 | scenario.timeConflict.secondCase | info |
| cancelEarliest.hasReplacement | 取消目前最早預約，只有一筆次早預約可補位發簡訊。 | scenario.cancelEarliest.singleReplacement | guide-confirm |
| cancelEarliest.hasReplacementConflict | 取消最早預約，有多筆同時間補位候選，自動選 ID 最小者。 | scenario.cancelEarliest.multiReplacement | guide-confirm |
| cancelEarliest.noReplacement | 取消最早預約後，群組內沒有任何預約可以補位。 | scenario.cancelEarliest.noReplacement | info |

---

### 4.2 跨群組操作（後端概念，前台文案沿用）

> 對使用者來說，他只是在「目前門市」新增預約。  
> 系統在後端會視情況使用下面的對應文案，  
> **文案內容與同群組情境完全共用，不另外維護一份。**

| 系統情境代號（RD 使用） | 說明（跨群組發生的時機） | 使用文案代號 | 彈窗模式 |
|-------------------------|--------------------------|--------------|----------|
| crossGroupAdd.emptyGroup | 新增到的目標群組目前沒有任何會發簡訊的預約，這是第一筆。 | scenario.becomeFirst.noPrevious | guide-confirm |
| crossGroupAdd.earlierThanExisting | 目標群組內已經有預約，新預約時間更早，會變成新的最早、取代原排程。 | scenario.becomeFirst.replaceExisting | guide-confirm |
| crossGroupAdd.laterThanExisting | 目標群組內已有更早預約負責發簡訊，新預約時間較晚，只新增、不發簡訊。 | scenario.later.hasEarlier | guide-cancel |
| crossGroupAdd.conflict | 目標群組內已有同時間預約，依先來後到（ID 較小）優先，新預約不發簡訊。 | scenario.timeConflict.sameTime | info |

> ✅ 重點：  
> - PM **只維護一次**文案（在第 2 章的 9 行情境）。  
> - 系統在「同群組 / 跨群組」兩邊會重複使用這些文案。  
> - 使用者主觀感受永遠是：「在目前門市處理當下這筆預約與簡訊」。

---

## 5. 驗收心法（給 PM）

1. **先看文案是否說人話**
   - 不要出現「群組 / SYSGroup」這種技術字眼。
   - 以「目前預約安排」、「更早預約」、「同時間預約」這種描述為主。

2. **再看行為是否符合邏輯**
   - 同一個 SYSGroup 裡，任何操作後都應該只剩一筆「最早預約」是會發簡訊的（`scheduled` / `delivered`）。  
   - 其他預約簡訊狀態應為 `none`（或異常狀態如 `failed`）。

3. **最後確認跨群組是否「只影響自己」**
   - 在某門市畫面操作時，只影響「該門市所屬群組」的簡訊狀態。  
   - 其他門市 / 群組的預約與簡訊排程不會被改動。

> ✅ 只要這三點都成立，文案與行為就可以視為已通過 PM 驗收。

---

## 6. 單筆預約簡訊狀態管理介面（後台調整用）

> 這個介面是「針對單一預約 ID，直接調整簡訊狀態」用的後台工具。  
> 例如：人工將某筆從 `failed` 改回 `scheduled`，或把 `scheduled` 改成 `cancelled`。

### 6.1 總原則

1. **仍然遵守「同一 SYSGroup 只有一筆最早預約可發簡訊」**
   - 即使是在單筆管理介面，只要將某筆改成「會發簡訊」的狀態（例如 `scheduled`），  
     系統仍會視為「這筆想要成為該群組的最早預約」，需要處理原本的最早排程。

2. **能不跳彈窗就不跳彈窗**
   - 純粹在「發送類」以外狀態間切換（`none` ↔ `cancelled` ↔ `failed`），不跳提示，直接更新即可。
   - 只有當「這次操作會影響『最早預約』邏輯」時，才會套用前面已定義的彈窗文案。

3. **文案盡量沿用既有情境**
   - 不另外為「單筆管理」寫一整套新文案，而是重用第 2 章的 9 種情境字詞。

---

### 6.2 單筆狀態操作行為表（邏輯說明）

> 本表給 PM / RD 對齊邏輯用，不一定都要有彈窗文案。  
> 真正需要文案的情境，會在下一小節指定「沿用哪一個」。

| 操作類型 | 原狀態 | 新狀態 | 是否影響「最早預約」邏輯 | 行為說明 |
|----------|--------|--------|--------------------------|----------|
| 直接標記為不發送 | `scheduled` / `delivered` | `cancelled` / `none` | ✅ 會 | 結束這筆的簡訊排程。若這筆剛好是最早預約，系統會自動尋找下一筆可補位的預約（邏輯同「取消最早」情境 #7~#9，必要時才走彈窗）。 |
| 修正異常為不發送 | `failed` | `none` / `cancelled` | ❌ 不會 | 單純視為「這筆不再處理」，不需要更動其他預約。 |
| 將某筆改為發送（原本不是最早） | `none` / `cancelled` / `failed` | `scheduled` | ✅ 會 | 等同於「讓這筆成為新的最早預約」，需視同「成為目前最早預約（取代舊排程）」。 |
| 將最早預約重新排程 | `failed` / `cancelled` / `none`（且這筆是時間上最早） | `scheduled` | ✅ 會 | 若這筆確實是該群組時間上最早，視為「修正最早預約異常」。 |
| 在非最早預約之間切換非發送狀態 | `none` / `cancelled` / `failed` | 三者互換 | ❌ 不會 | 完全不影響哪一筆是最早預約，也不影響發送對象，不需彈窗。 |

---

### 6.3 單筆管理介面會用到的「情境文案」

> ✅ 不新增新的文案，只指定「**這些動作時請沿用哪一個情境**」。  
> ✅ PM 維護文案時，仍只要編輯第 2 章的 9 個情境即可。

| 單筆操作情境 | 說明 | 使用的文案代號（沿用） | 彈窗模式 |
|--------------|------|------------------------|----------|
| 將某筆（且不是時間上最早）改成 `scheduled` | 使用者在單筆管理介面，直接把一筆非最早預約改成「要發簡訊」，這會讓它變成新的最早預約，並取代原本最早的排程。 | scenario.becomeFirst.replaceExisting | guide-confirm |
| 將一筆「時間上最早」但簡訊異常的預約改回 `scheduled` | 例如：原本是 `failed` / `cancelled`，現在要重新啟用這筆作為最早預約。 | scenario.earliest.abnormalFix | guide-confirm |
| 在單筆介面中，取消目前最早預約的 `scheduled`（且沒有可補位預約） | 視實作決策：若需要明示沒有補位可用時，可以沿用「取消最早後，沒有補位」文案。 | scenario.cancelEarliest.noReplacement | info |

> 其他狀況（例如：把 `failed` 改成 `none`，或在 `none` / `cancelled` 之間切換），  
> 只要不會違反「同一群組只有一筆會發簡訊」的核心規則，就不需要跳彈窗。

---

## 7. 一句話總結（給 PM 備註）

- 你只要維護 **第 2 章的 9 種情境文案 + 第 3 章的黑名單文案**。  
- 其餘全部都是：  
  > 「在不同操作（同群組 / 跨群組 / 單筆管理）時，  
  > 由系統決定要套用哪一段既有的文案」。
